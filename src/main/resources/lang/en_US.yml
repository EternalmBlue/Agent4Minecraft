common:
  no_permission: "You do not have permission to use this command."
  invalid_request: "invalid request"
  unknown_error: "unknown error"
  unknown: "unknown"
  empty: "<empty>"

ask:
  usage: "Usage: /{label} <question>"
  queued: "Requesting the remote backend, please wait..."
  progress:
    waiting: "Building the answer ({elapsed_seconds}s), waiting for backend progress..."
    running: "Building the answer ({elapsed_seconds}s), current step: {stage}"
    stage:
      started: "question accepted"
      memory_recall: "reading long-term memory"
      memory_ready: "long-term memory ready"
      agent_running: "planning retrieval and answer generation"
      retrieval: "searching plugin documentation"
      retrieval_ready: "plugin documentation search complete"
      server_config_retrieval: "searching server configuration memory"
      server_config_ready: "server configuration memory ready"
      answer_ready: "answer generation complete"
      memory_write: "recording this turn"
      completed: "completed"
  failed_fallback: "Ask request failed. Please retry later."
  question_blank: "Question must not be blank."
  already_running: "You already have an ask request in progress. Please wait for the result."
  cooldown: "You are sending requests too quickly. Please retry in {seconds} second(s)."

answer:
  header: "AI Answer:"
  empty: "The backend did not return any displayable content."

admin:
  usage: "Usage: /{label} <sync|status>"
  unknown_subcommand: "Unknown subcommand. Usage: /{label} <sync|status>"

sync:
  already_running: "A sync task is already running."
  starting: "Starting config sync..."
  failed_fallback: "Sync failed. Please retry later."
  finished: "Sync finished: syncId={sync_id}"
  summary: "Scanned={scanned}, requiredUploads={required_uploads}, uploaded={uploaded}"
  commit_summary: "Accepted={accepted}, indexed={indexed}, refreshStarted={refresh_started}"
  phase:
    scanning: "Scanning files to sync."
    preparing: "Sending the sync manifest to the backend."
    uploading: "Uploading {uploaded}/{total} required files."
    committing: "Uploads finished. Requesting backend commit."

status:
  query_queued: "Querying sync status..."
  query_failed_fallback: "Status query failed. Please retry later."
  backend_endpoint: "Backend endpoint: {endpoint}"
  local_sync_idle: "Local sync: idle"
  local_sync: "Local sync: {sync_id} / {phase}"
  local_message: "Local message: {message}"
  local_scanned_files: "Local scanned files: {count}"
  local_upload_progress: "Local upload progress: {uploaded_files}/{required_files} files, {uploaded_bytes}/{total_bytes}"
  started_at: "Started at: {timestamp}"
  remote_state: "Remote state: {state} / accepted={accepted} / indexed={indexed} / refresh={refresh_started}"
  remote_upload_progress: "Remote upload progress: {uploaded_files}/{required_files} files, {uploaded_bytes}/{total_bytes}"
  remote_current_upload: "Remote current upload: {path}"
  remote_refresh_progress: "Remote refresh progress: {completed}/{total} bundles, failed={failed}"
  remote_current_refresh: "Remote current refresh: {bundle} ({phase})"
  remote_message: "Remote message: {message}"
  remote_query_failed: "Remote status query failed: {error}"
  last_sync: "Last sync: syncId={sync_id}, scanned={scanned}, uploaded={uploaded}"
  last_commit: "Last commit: accepted={accepted}, indexed={indexed}, refresh={refresh_started}"
  last_completed: "Last completed: {timestamp}"
  last_failure: "Last failure: {message}"
  pending_sync_id: "pending-sync-id"
  phase:
    scanning: "SCANNING"
    preparing: "PREPARING"
    uploading: "UPLOADING"
    committing: "COMMITTING"
  remote_state_value:
    unknown: "UNKNOWN"
    pending: "PENDING"
    uploading: "UPLOADING"
    indexing: "INDEXING"
    completed: "COMPLETED"
    failed: "FAILED"

backend:
  message: "Backend message: {message}"
  action:
    probe: "backend probe"
    ask: "ask request"
    sync_prepare: "sync prepare"
    upload: "upload {path}"
    sync_commit: "sync commit"
    sync_status: "sync status query"
  error:
    transport_failure: "{action} failed: {detail}"
    authentication_failed: "Backend authentication failed. Check backend.authToken."
    permission_denied: "Backend denied {action}."
    timed_out: "{action} timed out. Please retry later. If a config sync is still refreshing, wait and retry."
    unavailable: "Could not reach the backend service. Check the network and backend status."
    request_rejected: "{action} was rejected by the backend: {detail}"
    upload_timed_out: "Upload for {path} timed out."
    file_read_failed: "Failed to read local file {path}."
    upload_interrupted: "Upload for {path} was interrupted."
    no_upload_result: "Backend did not return a file upload result."
    no_ask_result: "Backend did not return an ask result."

startup:
  probe_ack_false: "Backend probe returned ack=false."
  protocol_mismatch: "Bridge protocol version mismatch."
  probe_failed_default: "Backend probe failed."
  backend_unavailable: "Backend response: unavailable"
  backend_response: "Backend response: name={backend_name}, version={backend_version}, protocolVersion={protocol_version}, message={message}"
  failed: "Agent4Minecraft startup probe failed."
  attempted_endpoint: "Attempted backend endpoint: {endpoint}"
  server_identity: "Server identity: server.id={server_id}, serverInstanceId={server_instance_id}"
  plugin_identity: "Plugin identity: {plugin_name}@{plugin_version}"
  plugin_protocol: "Plugin bridge protocol version: {protocol_version}"
  failure_detail: "Failure detail: {detail}"
  check_plugin_config: "Check plugin config.yml: backend.host (if overriding localhost) and backend.port"
  check_backend_config: "Check backend config.toml or environment variables: grpc.host and grpc.port"
  confirm_backend_running: "Confirm the backend gRPC service is running."
  backend_entrypoint: "Start backend with: {entrypoint}"
  restart_server: "After fixing the configuration, restart the Minecraft server."
